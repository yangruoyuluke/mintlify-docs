I have identified the issues with the "CN" and "JP" paths:

1. **CN Files**: The files in the `cn/` directory currently have the `.txt` extension, but they contain MDX content (Markdown with React components) and are referenced in `docs.json` as documentation pages. They need to be renamed to `.mdx`.

Here is the plan to fix these issues:

**Rename CN Files**:

* Rename all `.txt` files in the `cn/` directory to `.mdx`.


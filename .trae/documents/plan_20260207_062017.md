# Git Push Plan

I noticed that `cn/run_avatar.mdx` has been modified. I will proceed with the following steps to sync your changes:

1.  **Stage Changes**: Add the modified `cn/run_avatar.mdx` to the staging area.
2.  **Commit**: Create a commit with the message "Update run_avatar.mdx".
3.  **Push**: Push the changes to `origin main`.
    *   *Note*: I will attempt a direct push first as it succeeded previously. If that fails due to network issues, I will attempt using the proxy as per your configuration.

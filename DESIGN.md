# MarsMind 文档设计规范

更新时间：2026-05-22

## 设计原则

MarsMind 文档站采用克制、清晰、可执行的后台操作指南风格。视觉与信息组织参考包豪斯的功能优先原则，并保持接近 Apple 和 OpenAI 的简洁表达。

## 内容结构

- 每个后台页面必须先说明功能目的，再说明操作路径。
- 操作步骤优先使用 Mintlify 的 `<Steps>` 和 `<Step>` 组件。
- 风险、权限、数据导出、删除、保存类动作必须使用 `<Warning>` 或 `<Info>` 提醒。
- 截图只展示用户定位操作所需的区域，避免无关内容堆叠。
- 页面标题使用直接的业务名词，例如“知识库管理”“数据洞察”。

## 截图规范

- 截图统一放在 `/images/admin-backend/`。
- 图片引用必须使用 root-relative 路径，例如 `/images/admin-backend/02-dashboard-business-performance.png`。
- 每张图片必须有明确 alt 文本，说明页面和关键控件。
- 后台截图应尽量保留左侧导航和当前主操作区，帮助用户判断入口位置。
- 涉及授权码、密码、客户隐私和外部联系人信息时，不展示敏感值。

## 文案规范

- 使用第二人称或直接祈使句，避免营销化表达。
- 操作指南要像在引导一个真实用户完成工作：先解释为什么这样做，再告诉他如何判断、调整和优化。
- 避免把显而易见的点击路径写成冗长步骤；步骤应承载判断标准、注意事项或能提升 AI 协作效果的操作经验。
- 全站中文文案默认使用“你”，保持自然对话感；API 字段、指标定义等参考型内容可以保留表格，但表格前后必须说明使用场景和判断方法。
- 每个表格只承载一类信息，例如“控件说明”或“字段建议”。
- 避免过长段落，复杂流程拆成步骤。
- 中英文产品名保持一致，例如 MarsMind、AI、QA、URL。

## 组件规范

- 普通说明使用 `<Info>`。
- 补充提示使用 `<Tip>`。
- 注册限制、导出数据、删除知识等风险事项使用 `<Warning>`。
- 截图使用 `<Frame>` 包裹。
- 并列配置项使用 Markdown 表格。
